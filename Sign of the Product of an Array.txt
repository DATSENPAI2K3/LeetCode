/**
 * @param {number[]} nums
 * @return {number}
 */
var arraySign = function(nums) {
    let x = BigInt(nums[0]);
    for(let i = 1; i < nums.length; i++){
        x = x * BigInt(nums[i]);
    }
    if(x > 0) return 1;
    if(x < 0) return -1;
    else return 0;
};